import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.coffeeCoder.mwebApi",
  appName: "MwebFibreApi",
  webDir: "build",
  bundledWebRuntime: false
};

export default config;
